# Local dev — Vite runs on localhost:5173, API on Laravel below
VITE_APP_DEFAULT_PATH=http://127.0.0.1:8000
REACT_APP_DEFAULT_PATH=http://127.0.0.1:8000

# Domain is auto from window.location (localhost in dev).
# VITE_APP_DOMAIN / REACT_APP_DOMAIN are optional overrides only.
